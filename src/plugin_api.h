// Adapter (forced to local shim for now) - prefer local shims to avoid SDK mismatches
#ifndef PLUGIN_API_ADAPTER_H
#define PLUGIN_API_ADAPTER_H

#include "shims/plugin_api.h"

#endif // PLUGIN_API_ADAPTER_H
